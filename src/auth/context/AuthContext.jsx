import { createContext } from "react";

// Cuando se trabaja con contextos, es conveniente hacer uso de reducers para definir las posibles acciones a ser despachadas
export const AuthContext = createContext();